import s from "../styles/Player.module.css"
import { useState } from "react"

export default function PlayerOne(){

    return(
        <div 
            className={s.player}

        >
        </div>
    )
}